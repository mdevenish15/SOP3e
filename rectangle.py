# the rectangle module has functions that perform calculations related to rectangles

# the area function accepts a rectangles width and length as arguments and returns the rectangles area
def area(width, length):
    return width * length

# the perimeter function accepts a rectangles width and length as arguments and returns the rectangles perimeter
def perimeter(width, length):
    return 2 * (width + length)
