# the circle module has functions that perform calculations related to circles
import math

# the area function accepts a circles radius as an argument and returns the area of the circle
def area(radius):
    return math.pi * radius**2

# the circumference function accepts a circles radius and returns the circumference
def circumference(radius):
    return 2 * math.pi * radius

